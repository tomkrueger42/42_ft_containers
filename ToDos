vector:
	all three insert() overloads needs to have a const_iterator not only iterator

iterators:
	reverse_iterator testing

map:
	what to do with _alloc? it is currently not in use in my case.
	implement _comp

iterators:
	const <-> non-const
